# ── Core LLM / RAG stack ──────────────────────────────────────────────────────
langchain==0.3.0
langchain-community==0.3.0
langchain-openai==0.2.0
langchain-ollama==0.2.0
langchain-core==0.3.0
openai==1.45.0
chromadb==0.5.3

# ── PDF parsing ───────────────────────────────────────────────────────────────
# Docling: layout-aware PDF parsing (preserves tables and headers locally)
# Heavy install (~1GB). Comment out and use pypdf alone if on constrained CI.
docling==2.5.0
pypdf==4.3.1          # Fallback parser used automatically if Docling unavailable

# ── ETL / Data layer ──────────────────────────────────────────────────────────
polars==1.4.0          # Fast DataFrame operations for Bronze/Silver layers
pyarrow==17.0.0        # Parquet I/O backend for Polars

# ── Schema validation ─────────────────────────────────────────────────────────
pydantic==2.8.0

# ── UI ────────────────────────────────────────────────────────────────────────
gradio==5.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tiktoken==0.7.0
python-dotenv==1.0.1
huggingface_hub==0.24.0

# ── Dev / CI (not needed in production container) ─────────────────────────────
ruff==0.6.0
mypy==1.11.0
pytest==8.3.0
